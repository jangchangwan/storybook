import React from 'react';
import Authority from '../components/Authority';

export default {
  component: Authority,
  title: 'CSS/Authority',
};

const Template = () => <Authority />;
export const Default = Template.bind({});
